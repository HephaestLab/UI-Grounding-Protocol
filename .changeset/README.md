# Changesets

Every user-visible package change must include a changeset. Documentation-only,
test-only, and repository-maintenance changes do not need one.

Run `pnpm changeset`, select the affected packages, and describe the change from
the consumer's perspective.
