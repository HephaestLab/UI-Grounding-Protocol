# Cross-domain transfer task: phase two

Additional condition information, if any, is now present in `held-out/GUIDE.md`.
Re-read the held-out input and write a complete `answer.final.json` conforming
to `answer.schema.json`. Do not alter `answer.initial.json` or the examples.
