# Document selection export

- `focus.items` is the application-owned selection.
- `catalog/uid` is the canonical identity; `kindUri` is the business type.
- `attestedBy: document-model` maps to authoritative evidence.
- `selectionStatus: settled` with one item means it is the unique primary entity
  and can be answered without clarification.
