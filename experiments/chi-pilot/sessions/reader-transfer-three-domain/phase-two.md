# Cross-application interpretation session — phase 2

The second phase has started. If `ADAPTATION.md` is now present, read it as an
application-specific representation guide. If it is absent, no additional
representation information is available.

Re-evaluate `held-out/input.json` while preserving all phase-one materials and
write only a JSON object to `answer.final.json` that conforms to
`answer.schema.json`. Do not modify `answer.initial.json`.
