# Cross-application interpretation session — phase 1

Work only inside this participant directory. Do not inspect parent directories,
the source repository, git history, other runs, or the network.

1. Review the two worked examples in `examples/`. Each contains an input and its
   correct answer. A representation guide may be shared at the participant root
   or placed inside an example.
2. Interpret `held-out/input.json` using only the materials currently present.
3. Write only a JSON object to `answer.initial.json` that conforms to
   `answer.schema.json`.
4. Do not create `answer.final.json` during this phase.

Use canonical `namespace/id` identities only when the available representation
supports them. Do not infer canonical IDs, authority, ambiguity, or freshness
from visible wording alone.
